abstract final class AppDurations {
  static const Duration photoFadeIn = Duration(milliseconds: 1500);
  static const Duration placeholderPulse = Duration(milliseconds: 1250);
  static const Duration snackBar = Duration(seconds: 2);
}
