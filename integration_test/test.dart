import 'language_change_test.dart' as language_test;

// Note: integration_test requires a --target param, we cannot provide just
//  a directory name. Therefore we call all the tests from here.
void main() {
  language_test.main();
}
